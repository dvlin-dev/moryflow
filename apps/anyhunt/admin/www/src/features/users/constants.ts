/**
 * [PROVIDES]: users 列表展示映射常量
 * [DEPENDS]: subscription badge 映射
 * [POS]: users 页面与子组件复用
 */

import { getSubscriptionTierBadgeVariant } from '@/lib/subscription-badges';

export { getSubscriptionTierBadgeVariant as getUserTierBadgeVariant };
