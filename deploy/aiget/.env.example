## Aiget (8c16g) deploy env
## Copy to `.env` and fill values.
##
## [必选] = 服务运行必须配置
## [可选] = 有默认值或功能可选

# -----------------------------------------------------------------------------
# Public domains
# -----------------------------------------------------------------------------
# [必选] 服务公网 URL
PUBLIC_BASE_URL=https://aiget.dev
SERVER_URL=https://aiget.dev

# [必选] CORS 允许的前端来源（逗号分隔）
ALLOWED_ORIGINS=https://console.aiget.dev,https://admin.aiget.dev,https://aiget.dev

# [必选] Better Auth 信任的来源（用于 CSRF 保护，逗号分隔）
TRUSTED_ORIGINS=https://console.aiget.dev,https://admin.aiget.dev,https://aiget.dev

# -----------------------------------------------------------------------------
# Ports (host)
# -----------------------------------------------------------------------------
AIGET_SERVER_PORT=3100
AIGET_WWW_PORT=3103
AIGET_CONSOLE_PORT=3102
AIGET_ADMIN_PORT=3101
AIGET_DOCS_PORT=3110

# Database / Redis ports exposed on host (for local debugging)
AIGET_POSTGRES_PORT=5432
AIGET_REDIS_PORT=6379

# -----------------------------------------------------------------------------
# Postgres (主库 - 业务数据)
# -----------------------------------------------------------------------------
POSTGRES_DB=aiget
POSTGRES_USER=aiget
# [必选] 改成强密码
POSTGRES_PASSWORD=change_me

# [必选] Prisma 主库连接字符串（密码与上面保持一致）
DATABASE_URL=postgresql://aiget:change_me@aiget-postgres:5432/aiget?schema=public

# -----------------------------------------------------------------------------
# Postgres Vector (向量库 - Memox 数据: Memory/Entity/Relation)
# -----------------------------------------------------------------------------
VECTOR_POSTGRES_DB=aiget_vector
VECTOR_POSTGRES_USER=aiget_vector
# [必选] 改成强密码（应与主库密码不同）
VECTOR_POSTGRES_PASSWORD=change_me

# [必选] Prisma 向量库连接字符串（密码与上面保持一致）
VECTOR_DATABASE_URL=postgresql://aiget_vector:change_me@aiget-vector-postgres:5432/aiget_vector?schema=public

# 向量库端口（默认 5433，避免与主库冲突）
AIGET_VECTOR_POSTGRES_PORT=5433

# -----------------------------------------------------------------------------
# Redis
# -----------------------------------------------------------------------------
REDIS_URL=redis://aiget-redis:6379

# -----------------------------------------------------------------------------
# Better Auth
# -----------------------------------------------------------------------------
# [必选] Better Auth 密钥（用于 JWT 签名）
# 生成方法: openssl rand -base64 32
BETTER_AUTH_SECRET=

# [必选] Better Auth URL（与 PUBLIC_BASE_URL 一致）
BETTER_AUTH_URL=https://aiget.dev

# [必选] 管理员密码（用于 admin 后台登录）
ADMIN_PASSWORD=

# -----------------------------------------------------------------------------
# Frontend build-time env (Vite embeds these)
# -----------------------------------------------------------------------------
VITE_API_URL=https://aiget.dev
VITE_AUTH_URL=https://aiget.dev
# [可选] Cloudflare Turnstile Site Key（人机验证）
VITE_TURNSTILE_SITE_KEY=

# -----------------------------------------------------------------------------
# Optional: Cloudflare Turnstile (验证码)
# -----------------------------------------------------------------------------
# TURNSTILE_SITE_KEY=
# TURNSTILE_SECRET_KEY=

# -----------------------------------------------------------------------------
# Optional: Resend (邮件服务)
# -----------------------------------------------------------------------------
# RESEND_API_KEY=
# EMAIL_FROM=Aiget <noreply@aiget.dev>

# -----------------------------------------------------------------------------
# Optional: OpenAI (AI Extract 功能)
# -----------------------------------------------------------------------------
# OPENAI_API_KEY=
# OPENAI_BASE_URL=
# OPENAI_DEFAULT_MODEL=gpt-4o-mini

# -----------------------------------------------------------------------------
# Optional: SearXNG (搜索功能)
# -----------------------------------------------------------------------------
# SEARXNG_URL=

# -----------------------------------------------------------------------------
# Optional: Cloudflare R2 (云存储)
# -----------------------------------------------------------------------------
# R2_ACCOUNT_ID=
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_BUCKET_NAME=
# R2_PUBLIC_URL=

# -----------------------------------------------------------------------------
# Optional: Creem (支付)
# -----------------------------------------------------------------------------
# CREEM_API_KEY=
# CREEM_WEBHOOK_SECRET=
# CREEM_TEST_MODE=true

# -----------------------------------------------------------------------------
# Optional: Webhook
# -----------------------------------------------------------------------------
# WEBHOOK_SECRET=
